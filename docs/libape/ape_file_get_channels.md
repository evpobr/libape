# ape_file_get_channels

Gets channels count of APE file.

## Syntax

```c
APE_EXPORT int ape_file_get_channels(ape_file *ctx);
```

### Parameters

`ctx` **in**  
APE context.

## Return value

Return value >= `0` indicates success, error otherwise.
